export interface Entry {
date: Date;
content: string;
}

/*
    Interace for defining structure/types of objects within application code - a way
    to enforce type safety

    Removed during transpilation (does not exist at runtime)
*/